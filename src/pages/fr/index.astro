---
// Redirect to / with French locale saved
---
<script is:inline>
  if (typeof localStorage !== 'undefined') {
    localStorage.setItem('locale', 'fr');
  }
  window.location.replace('/');
</script>
